project = "schedulör"
description = "A scheduling and tweening library."
full_description = [[A scheduling and tweening library.  
You should probably start out by reading `DSL` and then look at `curve`, `schedule` and `loop`...]]
file = {"dsl.moon", "sequences.moon", "curve.moon", "schedule.moon", "loop.moon", "compat.moon"}
no_return_or_parms = true
format = "markdown"
readme = "README.md"
