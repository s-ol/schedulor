project = "schedulör"
description = "A scheduling and tweening library."
full_description = [[A scheduling and tweening library.  
You should probably start out by reading `DSL` and then look at `curve` and `schedule`...]]
file = {"schedulor", exclude={"schedulor/init.moon"}}
no_return_or_parms = true
format = "markdown"
readme = "README.md"
